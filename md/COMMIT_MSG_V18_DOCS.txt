V18: add spec + refactor brief (planning docs)

Adds the V18 API adapter + TanStack Query refactor planning
artifacts to md/. The wave-by-wave tracker is staged separately
(landed inside V18.0.1 to keep phase tracking atomic).

Files:
- md/SIMBA_PLAYER_MODULE_V18_SPECIFICATION.md
  10-wave refactor plan, architecture layers, naming convention,
  risk register.
- md/V18_API_ADAPTER_REFACTOR_BRIEF.md
  Manager brief: the adapter is the fetcher function, not a
  per-API object; convertors are exported named helpers for
  testability and debuggability.

No code changes.
