V18: update tracker for V18.1.1 + V18.1.2 (Wave 1 closeout)

Wave 1 (Foundation) tracker update:

- V18.1.1 (10 items): all marked done. N/A for the
  `node -e require(...)` check (PowerShell can't require
  the .js; tsc 0 errors is the right gate).

- V18.1.2 (22 items): 13 marked done, 9 marked N/A. The N/A
  items all reference the OLD hook signature
  (`<TData, TArgs>(opts: {key, fetcher, args, ttlMs?, enabled?})`)
  that the type-contract redesign superseded. The new signature
  is `<TData, TError>(opts: {queryKey, queryFn, ...})` —
  junior-dev rule: 1 import, thin passthrough to TanStack.
  Also notes: QueryProvider (not QueryClientProvider) per
  the rename, and the V17 test cleanup that was bundled.

- Notes the bonus V17 test cleanup (AppButton/AppText
  tests' dead react-redux imports) for traceability.

No code changes.
