V20.8 — DRY lift useSectionOptions (3 copies → 1)

The 3 per-screen copies of `useSectionOptions` that lived at
  src/screens/MusicScreen/hooks/useOptions.ts
  src/screens/MoviesScreen/hooks/useOptions.ts
  src/screens/PodcastsScreen/hooks/useOptions.ts
were character-for-character identical (modulo a single 3-line
vs 1-line formatting difference in one branch of the Podcasts
copy). Each imported `SectionOptionGroupId`, `OptionGroup`,
`SectionOptionsMerged`, and `SectionBrowseConfig` from their
per-screen `../types`, and the 3 per-screen `types/index.ts`
defined all of these structurally identically.

The lift:
  1. New shared hook at `src/hooks/useSectionOptions.ts` — the
     one canonical implementation, importing the option types
     from the new `src/screens/_shared/sectionOptions.ts` and
     the minimal `SectionBrowseConfigOptions` subset from
     `src/screens/_shared/sectionBrowseConfig.ts` (now also
     extended to include the options shape; the search subset
     was added in V20.7).
  2. New shared type file `src/screens/_shared/sectionOptions.ts`
     with `SectionOptionGroupId`, `OptionItem`, `OptionGroup`,
     `SectionOptionsMerged`. The 3 per-screen `types/index.ts`
     still define these locally (the V20.7 comment in the new
     shared file notes this is intentional; a full lift of the
     per-screen `types/index.ts` is deferred to a follow-up
     wave).
  3. The 3 per-screen `hooks/useOptions.ts` are deleted
     (parked at `md/_v20_8_dead_code/` as the established
     dead-code pattern, renamed to `.txt` so tsc ignores them).
  4. 6 consumer imports are re-pointed:
       - 3 `BrowseLayout.tsx` files: `SectionOptionsApi` from
         `'../hooks/useOptions'` → `'../../../hooks/useSectionOptions'`
       - 3 `index.tsx` files: `useSectionOptions` from
         `'./hooks/useOptions'` → `'../../hooks/useSectionOptions'`
     No other consumer change — the hook's public shape is
     preserved.

Net line count: -330 lines (3 × ~110 lines deleted) + ~110 lines
for the new shared file. Plus the 1 new shared-options type file
(~30 lines). Plus the 1-line extension of
`sectionBrowseConfig.ts` (+1 type). Plus 1-line import updates
in 6 consumers.

Verification:
  - `npx tsc --noEmit` clean.
  - `npx jest --forceExit` clean: 9 suites, 106 passed, 1 todo.

Files:
  A  src/hooks/useSectionOptions.ts                 (new shared hook)
  A  src/screens/_shared/sectionOptions.ts          (new shared types)
  M  src/screens/_shared/sectionBrowseConfig.ts     (extended)
  M  src/screens/MusicScreen/components/BrowseLayout.tsx
  M  src/screens/MusicScreen/index.tsx
  M  src/screens/MoviesScreen/components/BrowseLayout.tsx
  M  src/screens/MoviesScreen/index.tsx
  M  src/screens/PodcastsScreen/components/BrowseLayout.tsx
  M  src/screens/PodcastsScreen/index.tsx
  D  src/screens/MusicScreen/hooks/useOptions.ts    (→ md/_v20_8_dead_code/)
  D  src/screens/MoviesScreen/hooks/useOptions.ts   (→ md/_v20_8_dead_code/)
  D  src/screens/PodcastsScreen/hooks/useOptions.ts (→ md/_v20_8_dead_code/)
