/** V19 W0 Phase 0.4 — `DpadController` STUB. Wave 3.6 fills in. */
import * as React from 'react';
export const DpadController: React.FC<{children: React.ReactNode}> = () => null;
export default DpadController;
