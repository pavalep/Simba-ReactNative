/**
 * All Audio screen — grid of all audio tracks with sort toggle.
 */
const textContent = {
  headerTitle: 'All Audio',
  sortByTitle: 'title',
  sortByArtist: 'artist',
  emptyTitle: 'No audio files found.',
} as const;

export default textContent;
