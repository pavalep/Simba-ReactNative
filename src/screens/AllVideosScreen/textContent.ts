/**
 * All Videos screen — grid of all video tracks with sort toggle.
 */
const textContent = {
  headerTitle: 'All Videos',
  sortByTitle: 'title',
  sortByDate: 'date',
  emptyTitle: 'No videos found.',
} as const;

export default textContent;
