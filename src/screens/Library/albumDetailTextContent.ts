/**
 * Album Detail screen (Library) — track listing with Play All and duration.
 */
const textContent = {
  headerTitle: 'Album',
  playAll: 'Play All',
  trackSingular: 'track',
  trackPlural: 'tracks',
  durationFallback: '--:--',
  emptyTitle: 'No tracks found for this album.',
} as const;

export default textContent;
