/**
 * Splash screen — animated brand intro and first-launch scan prompt.
 */
const textContent = {
  subtitle: 'Simba Player',
  welcomeTitle: 'Welcome to Simba Player',
  welcomeBody:
    'Scan your media library to discover videos and music. Or skip and browse later.',
  scanButton: 'Scan Media Library',
  skip: 'Skip for now',
} as const;

export default textContent;
