/**
 * Genre screen — tracks filtered by genre with count.
 */
const textContent = {
  headerTitle: 'Genre',
  trackSingular: 'track',
  trackPlural: 'tracks',
  durationFallback: '--:--',
  emptyTitle: 'No tracks found in this genre.',
} as const;

export default textContent;
