feat(v13-phase-55): delete 17 V11 audio components

V13 Phase 55: the V11 audio module under src/modules/playback/audio/
is dead code in the V12+ world (the activity branch renders
<PlayerRoot /> from the module; the MainActivity branch
renders the regular navigator). Deleted all 17 files:

  AudioArtwork.tsx
  AudioButton.tsx
  AudioIcon.tsx
  AudioMiniProgress.tsx
  AudioModule.tsx
  AudioOutputControl.tsx
  AudioPlaybackControllerContext.tsx
  AudioPlayer.tsx
  AudioPriorityActions.tsx
  AudioProgress.tsx
  AudioTransportControls.tsx
  AudioTypes.ts
  index.tsx (audio barrel)
  MiniAudio.tsx
  rangeNormalization.ts
  hooks/useAudioPlayerScreen.ts

The 17 files were moved to v13-trash-2026-09-04/ at the project
root (safety policy: no direct delete; the trash dir is
git-ignored via .gitignore addition and excluded from tsconfig
to keep typecheck clean). They can be removed with `git rm -r
v13-trash-2026-09-04/` after Phase 58.

Cross-reference fixes:
  - rangeNormalization (the cache-range normalizer for the
    seek-bar's grey "downloaded" overlay) is audio-agnostic,
    so it was promoted to src/utils/bufferedRanges.ts. The
    consumer's contexts/TransportContext.tsx now imports from
    the new location.
  - src/modules/playback/PlaybackOverlayHost.tsx still imports
    the doomed video module for its in-flight fallback path,
    but the V11 audio imports were removed. The component's
    render body now returns null (the App.tsx doesn't render
    it after Phase 54 anyway). Phase 57 will fully delete
    PlaybackOverlayHost + the doomed video module.

Verification:
  - npx tsc --noEmit: clean.
  - npm test: 203/206 pass (same 2 pre-existing V11 video tests).

Remaining V11 dead code (Phase 56/57):
  - src/native/ (Phase 56)
  - src/modules/playback/PlaybackContext.tsx (Phase 57)
  - src/modules/playback/PlaybackOverlayHost.tsx (Phase 57)
  - src/modules/playback/types.ts (Phase 57)
  - src/modules/playback/index.ts (Phase 57)
  - src/modules/playback/video/* (Phase 57)
  - src/contexts/TransportContext.tsx (Phase 57)
  - 14 V11 video test files (Phase 57)
